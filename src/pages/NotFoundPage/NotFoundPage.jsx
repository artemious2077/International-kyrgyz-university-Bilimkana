import { NotFoundComponent } from '@/modules/NotFoundComponent/NotFoundComponent'
import { ScrollArrow } from '@/UI/ScrollArrow/ScrollArrow'
export const NotFoundPage = () => {
  return (
    <main>
      <NotFoundComponent />
      <ScrollArrow />
    </main>
  )
}
